package word

import (
	"fmt"
	"testing"
)
func BenchmarkIsPalindrome(b *testing.B) {
	for i := 0; i < b.N; i++ {
		//IsPalindrome("A man, a plan, a canal: Panama")
		//fm
		fmt.Printf("yes")
	}
	//return true
}